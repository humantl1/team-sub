# Copy this file to `.env.local` and fill in each value with credentials from the Supabase dashboard.
# Vite exposes only variables that start with VITE_ to the browser build, so these names are required.
VITE_SUPABASE_URL="https://your-project-id.supabase.co"
VITE_SUPABASE_ANON_KEY="replace-with-your-anon-key"
